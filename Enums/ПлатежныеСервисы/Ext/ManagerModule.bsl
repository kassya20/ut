///////////////////////////////////////////////////////////////////////////////////////////////////////
// Copyright (c) 2025, ООО 1С-Софт
// Все права защищены. Эта программа и сопроводительные материалы предоставляются 
// в соответствии с условиями лицензии Attribution 4.0 International (CC BY 4.0)
// Текст лицензии доступен по ссылке:
// https://creativecommons.org/licenses/by/4.0/legalcode
///////////////////////////////////////////////////////////////////////////////////////////////////////

#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияДанныхВыбора(ДанныеВыбора, Параметры, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;
	ДанныеВыбора         = Новый СписокЗначений();	// СписокЗначений из ПеречислениеСсылка.ПлатежныеСервисы
	ДоступныеЗначения    = ПлатежныеСистемыСлужебный.ДоступныеПлатежныеСистемы();
	
	// Отбор по параметрам поиска
	Если ПустаяСтрока(Параметры.СтрокаПоиска) Тогда
		ДанныеВыбора = ДоступныеЗначения;
	Иначе
		Для Каждого Элемент Из ДоступныеЗначения Цикл
			Представление = СтрНайтиИВыделитьОформлением(Элемент.Представление, Параметры.СтрокаПоиска);
			Если Представление <> Неопределено Тогда
				ДанныеВыбора.Добавить(Элемент.Значение, Представление);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли
