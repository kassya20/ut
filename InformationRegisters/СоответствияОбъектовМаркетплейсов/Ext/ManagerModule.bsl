
#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ПрограммныйИнтерфейс

#Область ДляВызоваИзДругихПодсистем

// СтандартныеПодсистемы.УправлениеДоступом

// Позволяет указать объекты метаданных, для которых задана логика ограничения доступа к данным.
//
// Параметры:
//   Ограничение - см. УправлениеДоступомПереопределяемый.ПриЗаполненииОграниченияДоступа.Ограничение.
//
Процедура ПриЗаполненииОграниченияДоступа(Ограничение) Экспорт

	Ограничение.Текст =
		"РазрешитьЧтениеИзменение
		|ГДЕ
		|	ЗначениеРазрешено(УчетнаяЗаписьМаркетплейса.Организация, ПустаяСсылка КАК Истина)
		|	И ЗначениеРазрешено(УчетнаяЗаписьМаркетплейса.Партнер, ПустаяСсылка КАК Истина)";
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.УправлениеДоступом

#КонецОбласти

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Определяет объект соответствия.
//
// Параметры:
//   УчетнаяЗапись        - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису.
//   ВидОбъекта           - ПеречислениеСсылка.ВидыОбъектовМаркетплейсов - вид объекта маркетплейса.
//   ИдентификаторОбъекта - Строка - идентификатор объекта маркетплейса.
//   ТипОбъекта           - Тип, Неопределено - ограничения по типу объекта.
//
// Возвращаемое значение:
//   Произвольный - объект соответствия.
//
Функция ПолучитьОбъектСоответствия(УчетнаяЗапись, ВидОбъекта, ИдентификаторОбъекта, ТипОбъекта = Неопределено) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ РАЗЛИЧНЫЕ ПЕРВЫЕ 2
		|	СоответствияОбъектовМаркетплейсов.Объект1С КАК Объект1С
		|ИЗ
		|	РегистрСведений.СоответствияОбъектовМаркетплейсов КАК СоответствияОбъектовМаркетплейсов
		|ГДЕ
		|	СоответствияОбъектовМаркетплейсов.УчетнаяЗаписьМаркетплейса = &УчетнаяЗапись
		|	И СоответствияОбъектовМаркетплейсов.ВидОбъектаМаркетплейса = &ВидОбъекта
		|	И СоответствияОбъектовМаркетплейсов.ИдентификаторОбъектаМаркетплейса = &ИдентификаторОбъекта";
	
	Запрос.УстановитьПараметр("УчетнаяЗапись",        УчетнаяЗапись);
	Запрос.УстановитьПараметр("ВидОбъекта",           ВидОбъекта);
	Запрос.УстановитьПараметр("ИдентификаторОбъекта", ИдентификаторОбъекта);
	
	УстановитьПривилегированныйРежим(Истина);
	ВыборкаДанных = Запрос.Выполнить().Выбрать();
	УстановитьПривилегированныйРежим(Ложь);
	
	Пока ВыборкаДанных.Следующий() Цикл
		Если ТипОбъекта = Неопределено Тогда
			Возврат ВыборкаДанных.Объект1С;
		ИначеЕсли ТипЗнч(ВыборкаДанных.Объект1С) = ТипОбъекта Тогда
			Возврат ВыборкаДанных.Объект1С;
		КонецЕсли;
	КонецЦикла;
	
	Возврат Неопределено;
	
КонецФункции

// Записывает объект соответствия.
//
// Параметры:
//   УчетнаяЗапись           - СправочникСсылка.УчетныеЗаписиМаркетплейсов - учетная запись подключения к сервису.
//   ВидОбъекта              - ПеречислениеСсылка.ВидыОбъектовМаркетплейсов - вид объекта маркетплейса.
//   ИдентификаторОбъекта    - Строка - идентификатор объекта маркетплейса.
//   Объект1С                - Произвольный - объект соответствия.
//   ДополнительныеПараметры - Неопределено -
//                           - Структура Из КлючИЗначение - значения прочих измерений и реквизитов регистра сведений 
//                               СоответствияОбъектовМаркетплейсов.
//
Процедура ЗаписатьОбъектСоответствия(УчетнаяЗапись, ВидОбъекта, ИдентификаторОбъекта, Объект1С, 
			ДополнительныеПараметры = Неопределено) Экспорт
	
	НачатьТранзакцию();
	
	Попытка
		Блокировка = Новый БлокировкаДанных();
		ЭлементБлокировки = Блокировка.Добавить("РегистрСведений.СоответствияОбъектовМаркетплейсов");
		ЭлементБлокировки.УстановитьЗначение("УчетнаяЗаписьМаркетплейса",        УчетнаяЗапись);
		ЭлементБлокировки.УстановитьЗначение("ВидОбъектаМаркетплейса",           ВидОбъекта);
		ЭлементБлокировки.УстановитьЗначение("ИдентификаторОбъектаМаркетплейса", ИдентификаторОбъекта);
		Если ДополнительныеПараметры <> Неопределено
				И ДополнительныеПараметры.Свойство("ИдентификаторВладельцаОбъектаМаркетплейса") Тогда
			ЭлементБлокировки.УстановитьЗначение("ИдентификаторВладельцаОбъектаМаркетплейса", 
				ДополнительныеПараметры.ИдентификаторВладельцаОбъектаМаркетплейса);
		КонецЕсли;
		ЭлементБлокировки.УстановитьЗначение("Объект1С", Объект1С);
		Блокировка.Заблокировать();        
		
		НаборЗаписей = СоздатьНаборЗаписей();
		НаборЗаписей.Отбор.УчетнаяЗаписьМаркетплейса.Установить(УчетнаяЗапись);
		НаборЗаписей.Отбор.ВидОбъектаМаркетплейса.Установить(ВидОбъекта);
		НаборЗаписей.Отбор.ИдентификаторОбъектаМаркетплейса.Установить(ИдентификаторОбъекта);
		Если ДополнительныеПараметры <> Неопределено
				И ДополнительныеПараметры.Свойство("ИдентификаторВладельцаОбъектаМаркетплейса") Тогда
			НаборЗаписей.Отбор.ИдентификаторВладельцаОбъектаМаркетплейса.Установить(
				ДополнительныеПараметры.ИдентификаторВладельцаОбъектаМаркетплейса);  
		КонецЕсли;
		НаборЗаписей.Отбор.Объект1С.Установить(Объект1С);
		НаборЗаписей.Прочитать();
		
		Если НаборЗаписей.Количество() = 0 Тогда
			Запись = НаборЗаписей.Добавить();
		Иначе
			Запись = НаборЗаписей[0];
		КонецЕсли;
		
		Если ДополнительныеПараметры <> Неопределено Тогда
			ЗаполнитьЗначенияСвойств(Запись, ДополнительныеПараметры);
		КонецЕсли;
		
		Запись.УчетнаяЗаписьМаркетплейса        = УчетнаяЗапись;
		Запись.ВидОбъектаМаркетплейса           = ВидОбъекта;
		Запись.ИдентификаторОбъектаМаркетплейса = ИдентификаторОбъекта;
		Запись.Объект1С                         = Объект1С;
		Если ДополнительныеПараметры = Неопределено Тогда
			Запись.ДатаАктуальности = ТекущаяДатаСеанса();
		КонецЕсли;
		
		НаборЗаписей.Записать(Истина);
		ЗафиксироватьТранзакцию();
		
	Исключение
		ОтменитьТранзакцию();
		
		ТекстОшибки = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
			НСтр("ru = 'При записи данных соответствия для объекта %1 возникла ошибка: %2'",
				ОбщегоНазначения.КодОсновногоЯзыка()),
			Объект1С,
			ОбработкаОшибок.ПодробноеПредставлениеОшибки(ИнформацияОбОшибке()));
			
		ЗаписьЖурналаРегистрации(ИнтеграцияСМаркетплейсамиСервер.СобытиеЖурналаРегистрации(),
			УровеньЖурналаРегистрации.Ошибка,,,
			ТекстОшибки);
	КонецПопытки;
	
КонецПроцедуры

#Область ОбновлениеИнформационнойБазы

// см. ОбновлениеИнформационнойБазыБСП.ПриДобавленииОбработчиковОбновления
Процедура ПриДобавленииОбработчиковОбновления(Обработчики) Экспорт
	
	Обработчик = Обработчики.Добавить();
	Обработчик.Процедура = "РегистрыСведений.СоответствияОбъектовМаркетплейсов.ОбработатьДанныеДляПереходаНаНовуюВерсию";
	Обработчик.Версия = "2.5.27.23";
	Обработчик.РежимВыполнения = "Отложенно";
	Обработчик.Идентификатор = Новый УникальныйИдентификатор("91b6e7e7-e764-45b2-8e39-b68252932aeb");
	Обработчик.Многопоточный = Истина;
	Обработчик.ПроцедураЗаполненияДанныхОбновления = "РегистрыСведений.СоответствияОбъектовМаркетплейсов.ЗарегистрироватьДанныеКОбработкеДляПереходаНаНовуюВерсию";
	Обработчик.ПроцедураПроверки = "ОбновлениеИнформационнойБазы.ДанныеОбновленыНаНовуюВерсиюПрограммы";
	Обработчик.Комментарий = СтроковыеФункцииКлиентСервер.ПодставитьПараметрыВСтроку(
		НСтр("ru = 'Дополняет настройки соответствия складов торговой площадки %1.'"),
		ИнтеграцияСМаркетплейсамиПовтИсп.ПредставлениеВидаТорговойПлощадки(
			Перечисления.ВидыМаркетплейсов.МаркетплейсЯндексМаркет));
	
	Изменяемые = Новый Массив; // Массив из Строка
	Изменяемые.Добавить(Метаданные.РегистрыСведений.СоответствияОбъектовМаркетплейсов.ПолноеИмя());
	Обработчик.ИзменяемыеОбъекты = СтрСоединить(Изменяемые, ",");
	
	Блокируемые = Новый Массив; // Массив из Строка
	Блокируемые.Добавить(Метаданные.РегистрыСведений.СоответствияОбъектовМаркетплейсов.ПолноеИмя());
	Обработчик.БлокируемыеОбъекты = СтрСоединить(Блокируемые, ",");
	
КонецПроцедуры

// Регистрирует данные регистра к обработке в отложенном обработчике обновления.
//
// Параметры:
//  Параметры - см. ОбновлениеИнформационнойБазы.ОсновныеПараметрыОтметкиКОбработке
//
Процедура ЗарегистрироватьДанныеКОбработкеДляПереходаНаНовуюВерсию(Параметры) Экспорт
	
	МетаданныеРегистра = Метаданные.РегистрыСведений.СоответствияОбъектовМаркетплейсов;
	
	Запрос = Новый Запрос;
	Запрос.Текст = 
		"ВЫБРАТЬ
		|	Сведения.УчетнаяЗаписьМаркетплейса КАК УчетнаяЗаписьМаркетплейса,
		|	Сведения.ВидОбъектаМаркетплейса КАК ВидОбъектаМаркетплейса,
		|	Сведения.ИдентификаторОбъектаМаркетплейса КАК ИдентификаторОбъектаМаркетплейса,
		|	Сведения.ИдентификаторВладельцаОбъектаМаркетплейса КАК ИдентификаторВладельцаОбъектаМаркетплейса,
		|	Сведения.Объект1С КАК Объект1С
		|ИЗ
		|	РегистрСведений.СоответствияОбъектовМаркетплейсов КАК Сведения
		|ГДЕ
		|	Сведения.УчетнаяЗаписьМаркетплейса.ВидМаркетплейса = ЗНАЧЕНИЕ(Перечисление.ВидыМаркетплейсов.МаркетплейсЯндексМаркет)
		|	И Сведения.ВидОбъектаМаркетплейса = ЗНАЧЕНИЕ(Перечисление.ВидыОбъектовМаркетплейсов.Склад)
		|	И Сведения.ИдентификаторВладельцаОбъектаМаркетплейса = """"
		|	И НЕ Сведения.ИспользуетсяДляСхемыРаботыFBS";
	
	РезультатЗапроса = Запрос.Выполнить();
	ИнтеграцияСМаркетплейсамиСервер.ОтметитьКОбработкеПриПереходеНаНовуюВерсию(
		Параметры,
		МетаданныеРегистра,
		РезультатЗапроса);
	
КонецПроцедуры

// Обработчик обновления.
//
// Параметры:
//  Параметры - Структура:
//    * Очередь - Число - очередь обработки, в которой выполняется текущий обработчик.
//    * ОбработкаЗавершена - Булево - признак окончания обработки.
//
Процедура ОбработатьДанныеДляПереходаНаНовуюВерсию(Параметры) Экспорт
	
	МетаданныеРегистра = Метаданные.РегистрыСведений.СоответствияОбъектовМаркетплейсов;
	ПолноеИмяРегистра = МетаданныеРегистра.ПолноеИмя();
	
	// Нельзя использовать метод создания временной таблицы для обработки, т.к. одно из измерений составного типа
	ОбновляемыеДанные = ОбновлениеИнформационнойБазы.ДанныеДляОбновленияВМногопоточномОбработчике(Параметры);
	
	Если ОбновляемыеДанные.Количество() = 0 Тогда
		Параметры.ОбработкаЗавершена = Истина;
		Возврат;
	КонецЕсли;
	
	ДанныеДляОбработки = ПорцияДанныхДляПереходаНаНовуюВерсию(МетаданныеРегистра, ПолноеИмяРегистра, ОбновляемыеДанные);
	
	ВключаемыеПоля = ИнтеграцияСМаркетплейсамиСервер.ВключаемыеПоляДляЗапросаЗначенийПолейОбъектаМетаданных(Ложь);
	ВключаемыеПоля.ВключатьИзмерения = Истина;
	
	ОбработанныеЗаписи = ИнтеграцияСМаркетплейсамиСервер.ПустаяТаблицаОбъектаМетаданных(
		ПолноеИмяРегистра,
		Истина,
		ВключаемыеПоля);
	
	ОбработатьПорциюДанныхДляПереходаНаНовуюВерсию(ДанныеДляОбработки, ОбработанныеЗаписи, МетаданныеРегистра);
	
	Параметры.ОбработкаЗавершена = 
		Не ОбновлениеИнформационнойБазы.ЕстьДанныеДляОбработки(Параметры.Очередь, МетаданныеРегистра.ПолноеИмя());
		
КонецПроцедуры

// Параметры:
//  МетаданныеРегистра - ОбъектМетаданныхРегистрСведений
//  ПолноеИмяРегистра - Строка
//  ТаблицаОбновляемыеДанные - ТаблицаЗначений - состав колонок таблицы совпадает с составом измерений регистра.
// 
// Возвращаемое значение:
//  ВыборкаИзРезультатаЗапроса
//
Функция ПорцияДанныхДляПереходаНаНовуюВерсию(МетаданныеРегистра, ПолноеИмяРегистра, ТаблицаОбновляемыеДанные)
	
	ТекстЗапроса =
		"ВЫБРАТЬ
		|	&ТекстЗапросаИзмеренияРегистра КАК ТекстЗапросаИзмеренияРегистра
		|ПОМЕСТИТЬ ВТИзмеренияРегистра
		|ИЗ
		|	&ТаблицаОбновляемыеДанные КАК ОбновляемыеДанные
		|;
		|
		|////////////////////////////////////////////////////////////////////////////////
		|ВЫБРАТЬ
		|	&ТекстЗапросаПоляРегистра КАК ТекстЗапросаПоляРегистра
		|ИЗ
		|	ВТИзмеренияРегистра КАК ИзмеренияРегистра
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ ПолноеИмяРегистра КАК ДанныеРегистра
		|		ПО (&ТекстЗапросаУсловиеСоединения)";
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "ПолноеИмяРегистра", ПолноеИмяРегистра);
	
	ИзмеренияРегистра = Новый Массив; // Массив из Строка
	УсловияСоединения = Новый Массив; // Массив из Строка
	
	Для Каждого Измерение Из МетаданныеРегистра.Измерения Цикл
		ТекстИзмерение = СтрШаблон("ОбновляемыеДанные.%1 КАК %1", Измерение.Имя);
		ИзмеренияРегистра.Добавить(ТекстИзмерение);
		
		ТекстСоединения = СтрШаблон("ИзмеренияРегистра.%1 = ДанныеРегистра.%1", Измерение.Имя);
		УсловияСоединения.Добавить(ТекстСоединения);
	КонецЦикла;
	
	ТекстЗапроса = СтрЗаменить(ТекстЗапроса, "&ТекстЗапросаУсловиеСоединения", СтрСоединить(УсловияСоединения, " И "));
	
	ТекстЗапросаИзмеренияРегистра = СтрСоединить(ИзмеренияРегистра, "," + Символы.ПС);
	ТекстЗапроса = СтрЗаменить(
		ТекстЗапроса,
		"&ТекстЗапросаИзмеренияРегистра КАК ТекстЗапросаИзмеренияРегистра",
		ТекстЗапросаИзмеренияРегистра);
		
	ТекстЗапроса = СтрЗаменить(
		ТекстЗапроса,
		"&ТекстЗапросаПоляРегистра КАК ТекстЗапросаПоляРегистра",
		ИнтеграцияСМаркетплейсамиСервер.ТекстЗапросаПолейОбъектаМетаданных(ПолноеИмяРегистра, "ДанныеРегистра"));
	
	Запрос = Новый Запрос;
	Запрос.Текст = ТекстЗапроса;
	Запрос.УстановитьПараметр("ТаблицаОбновляемыеДанные", ТаблицаОбновляемыеДанные);
	
	РезультатЗапроса = Запрос.Выполнить();
	Возврат РезультатЗапроса.Выбрать();
	
КонецФункции

Процедура ОбработатьПорциюДанныхДляПереходаНаНовуюВерсию(ДанныеДляОбработки, ОбработанныеЗаписи, МетаданныеРегистра)
	
	ПолноеИмяРегистра = МетаданныеРегистра.ПолноеИмя();
	
	ДополнительныеПараметры =
		ИнтеграцияСМаркетплейсамиСервер.ДополнительныеПараметрыОтметкиОбработкиПриПереходеНаНовуюВерсию(
			ПолноеИмяРегистра);
	
	НачатьТранзакцию();
	Попытка
		ПричинаИсключения = "Блокировка";
		
		Блокировка = Новый БлокировкаДанных;
		ИнтеграцияСМаркетплейсамиСервер.ДобавитьБлокировкуРегистраПоВсемИзмерениям(
			МетаданныеРегистра,
			Блокировка,
			ДанныеДляОбработки.Владелец());
		Блокировка.Заблокировать();
		
		ПричинаИсключения = "ПлохиеДанные";
		
		ОбновляемыеЗаписи = СоздатьНаборЗаписей();
		
		Пока ДанныеДляОбработки.Следующий() Цикл
			ЗаполнитьЗначенияСвойств(ОбработанныеЗаписи.Добавить(), ДанныеДляОбработки);
			
			НоваяСтрока = ОбновляемыеЗаписи.Добавить();
			ЗаполнитьЗначенияСвойств(НоваяСтрока, ДанныеДляОбработки);
			
			НоваяСтрока.ИспользуетсяДляСхемыРаботыFBS = Истина;
		КонецЦикла;
		
		ОбновляемыеЗаписи.Записать(РежимЗамещения.Обновление);
		
		ОбновлениеИнформационнойБазы.ОтметитьВыполнениеОбработки(ОбработанныеЗаписи, ДополнительныеПараметры);
		ЗафиксироватьТранзакцию();
	Исключение
		ОтменитьТранзакцию();
		
		НаборЗаписей = СоздатьНаборЗаписей();
		
		Если ОбработанныеЗаписи.Количество() > 0 Тогда
			Строка = ОбработанныеЗаписи[ОбработанныеЗаписи.Количество() - 1];
			Для Каждого Измерение Из МетаданныеРегистра.Измерения Цикл
				НаборЗаписей.Отбор[Измерение.Имя].Установить(Строка[Измерение.Имя]);
			КонецЦикла;
		КонецЕсли;
		
		//@skip-check invocation-parameter-type-intersect
		ОбновлениеИнформационнойБазыУТ.СообщитьОНеудачнойОбработке(ИнформацияОбОшибке(), НаборЗаписей);
		
		Если ПричинаИсключения = "ПлохиеДанные" Тогда
			ОбновлениеИнформационнойБазы.ОтметитьВыполнениеОбработки(ОбработанныеЗаписи, ДополнительныеПараметры);
		КонецЕсли;
	КонецПопытки;
	
КонецПроцедуры

#КонецОбласти

#КонецОбласти

#КонецЕсли
