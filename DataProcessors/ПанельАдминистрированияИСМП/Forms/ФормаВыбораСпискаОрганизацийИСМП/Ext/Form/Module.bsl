
#Область ОбработчикиСобытийФормы

&НаСервере
Процедура ПриСозданииНаСервере(Отказ, СтандартнаяОбработка)
	
	Если Параметры.Свойство("АвтоТест") Тогда
		Возврат;
	КонецЕсли;
	
	ЗаполнитьСписокОрганизаций();
	
	ОбщегоНазначенияСобытияФормИСПереопределяемый.ПриСозданииНаСервере(ЭтотОбъект, Отказ, СтандартнаяОбработка);
	
КонецПроцедуры

&НаСервере
Процедура ПриЗагрузкеДанныхИзНастроекНаСервере(Настройки)
	
	Настройки.Очистить();
	
	СохраненнаяТаблицаОрганизаций = Настройки.Получить("ТаблицаОрганизации");
	Если СохраненнаяТаблицаОрганизаций = Неопределено Тогда
		Возврат;
	КонецЕсли;
	
	Если ТипЗнч(СохраненнаяТаблицаОрганизаций) <> Тип("ТаблицаЗначений") Тогда
		Возврат;
	КонецЕсли;
	
	Для Каждого СтрокаТЧ Из ТаблицаОрганизации Цикл
		НайденнаяСтрока = СохраненнаяТаблицаОрганизаций.Найти(СтрокаТЧ.Организация, "Организация");
		Если НайденнаяСтрока = Неопределено Тогда
			СтрокаТЧ.Выбрана = Ложь;
		Иначе
			СтрокаТЧ.Выбрана = НайденнаяСтрока.Выбрана;
		КонецЕсли;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиСобытийЭлементовТаблицыФормыТаблицаОрганизации

&НаКлиенте
Процедура ТаблицаОрганизацииВыбор(Элемент, ВыбраннаяСтрока, Поле, СтандартнаяОбработка)
	
	СтандартнаяОбработка = Ложь;

	ТекущиеДанные = Элементы.ТаблицаОрганизации.ТекущиеДанные;
	Если ТекущиеДанные = Неопределено Тогда
		Возврат;
	КонецЕсли;

	ПоказатьЗначение(, ТекущиеДанные.Организация);
	
КонецПроцедуры

&НаКлиенте
Процедура ТаблицаОрганизацииЗагрузитьОстаткиПриИзменении(Элемент)
	
	ТекущаяСтрока = Элементы.ТаблицаОрганизации.ТекущиеДанные;
	
	Если ТекущаяСтрока.ЗагрузитьОстатки Тогда
		ТекущаяСтрока.Выбрана = Истина;
	КонецЕсли;
	
КонецПроцедуры

&НаКлиенте
Процедура ТаблицаОрганизацииВыбранаПриИзменении(Элемент)
	
	ТекущаяСтрока = Элементы.ТаблицаОрганизации.ТекущиеДанные;
	
	Если Не ТекущаяСтрока.Выбрана Тогда
		ТекущаяСтрока.ЗагрузитьОстатки = Ложь;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область ОбработчикиКомандФормы

&НаКлиенте
Процедура ВыбратьВсе(Команда)
	
	Для Каждого СтрокаТЧ Из ТаблицаОрганизации Цикл
		СтрокаТЧ.Выбрана          = Истина;
		СтрокаТЧ.ЗагрузитьОстатки = Истина;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура ИсключитьВсе(Команда)
	
	Для Каждого СтрокаТЧ Из ТаблицаОрганизации Цикл
		СтрокаТЧ.Выбрана          = Ложь;
		СтрокаТЧ.ЗагрузитьОстатки = Ложь;
	КонецЦикла;
	
КонецПроцедуры

&НаКлиенте
Процедура Выбрать(Команда)
	
	РезультатВыбора = Новый Соответствие;
	
	ПараметрыОтбора = Новый Структура;
	ПараметрыОтбора.Вставить("Выбрана", Истина);
	НайденныеСтроки = ТаблицаОрганизации.НайтиСтроки(ПараметрыОтбора);
	
	Для Каждого СтрокаТЧ Из НайденныеСтроки Цикл
		СтруктураВыбора = Новый Структура;
		СтруктураВыбора.Вставить("ЗагружатьТоварныеГруппы", Истина);
		СтруктураВыбора.Вставить("ЗагружатьНоменклатуру",   Ложь);
		
		Если СтрокаТЧ.ЗагрузитьОстатки Тогда
			СтруктураВыбора.ЗагружатьНоменклатуру = Истина;
		КонецЕсли;
		
		РезультатВыбора.Вставить(СтрокаТЧ.Организация, СтруктураВыбора);
		
	КонецЦикла;
	
	Закрыть(РезультатВыбора);
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

&НаСервере
Процедура ЗаполнитьСписокОрганизаций()
	
	Для Каждого ЭлементСписка Из ОбщегоНазначенияИС.ДоступныеОрганизации() Цикл
		НоваяСтрока = ТаблицаОрганизации.Добавить();
		НоваяСтрока.Организация = ЭлементСписка.Значение;
		НоваяСтрока.Выбрана = Параметры.Организации.Найти(ЭлементСписка.Значение) <> Неопределено;
	КонецЦикла;
	
КонецПроцедуры

#КонецОбласти

