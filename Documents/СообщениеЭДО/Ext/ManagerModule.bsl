#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда
	
#Область ПрограммныйИнтерфейс

// СтандартныеПодсистемы.УправлениеДоступом

// См. УправлениеДоступомПереопределяемый.ПриЗаполненииСписковСОграничениемДоступа.
Процедура ПриЗаполненииОграниченияДоступа(Ограничение) Экспорт
	
	Ограничение.Текст =
		"РазрешитьЧтение
		|ГДЕ
		|	ЧтениеОбъектаРазрешено(ЭлектронныйДокумент)
		|;
		|РазрешитьИзменениеЕслиРазрешеноЧтение
		|ГДЕ
		|	ИзменениеОбъектаРазрешено(ЭлектронныйДокумент)";
	
КонецПроцедуры

// Конец СтандартныеПодсистемы.УправлениеДоступом

#КонецОбласти

#Область ОбработчикиСобытий

Процедура ОбработкаПолученияПолейПредставления(Поля, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	Поля.Добавить("ТипЭлементаРегламента");
	Поля.Добавить("Номер");
	Поля.Добавить("Дата");
КонецПроцедуры

Процедура ОбработкаПолученияПредставления(Данные, Представление, СтандартнаяОбработка)
	СтандартнаяОбработка = Ложь;
	Представление = СтрШаблон(НСтр("ru = '%1 %2 от %3'"), Данные.ТипЭлементаРегламента, Данные.Номер, Данные.Дата);
КонецПроцедуры

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Возвращает проштампованный файл.
//
// Параметры:
//  СообщениеЭДО - ДокументСсылка.СообщениеЭДО
//  
// Возвращаемое значение:
//  Структура - Структура результата получения проштампованного файла:
//  * ПредставлениеДокумента - ДвоичныеДанные - Данные проштампованного файла.
//                           - Неопределено - Если файл не удалось получить.
//  * Успех                  - Булево - Истина, если удалось получить двоичные данные, иначе - Ложь.
//  * ТекстОшибки            - Строка - Текст ошибки.
//
Функция ПроштампованныйФайл(СообщениеЭДО) Экспорт
	
	Возврат ВизуализацияЭДОСлужебный.ПроштампованныйФайл(СообщениеЭДО);
	
КонецФункции

// Определяет, можно ли использовать предварительный просмотр в pdf для сообщения ЭДО
//
// Параметры:
//  СообщениеЭДО - ДокументСсылка.СообщениеЭДО
//  ПроверятьДоступностьВыводаПДФ_А3 - Булево - Проверять версию платформы для работы с просмотром файлов формата
//                                     PDF/A3. Требуется для того, чтобы не получать ошибку обновления динамического
//                                     списка. Следует передавать Истина, если проверка требуется.
// 
// Возвращаемое значение:
//  Булево - можно ли использовать предварительный просмотр в pdf.
//
Функция ИспользоватьПросмотрPDF(СообщениеЭДО, ПроверятьДоступностьВыводаПДФ_А3 = Ложь) Экспорт
	
	Возврат ВизуализацияЭДОСлужебный.ИспользоватьПросмотрPDF(СообщениеЭДО, ПроверятьДоступностьВыводаПДФ_А3);
	
КонецФункции

// Параметры:
//  ФайлСообщения - СправочникСсылка.СообщениеЭДОПрисоединенныеФайлы
// 
// Возвращаемое значение:
//  Структура:
//  * ЭлектронныйДокумент - ДокументСсылка.ЭлектронныйДокументВходящийЭДО,ДокументСсылка.ЭлектронныйДокументИсходящийЭДО
//  * ТипЭлементаРегламента - ПеречислениеСсылка.ТипыЭлементовРегламентаЭДО
//  * ИдентификаторОрганизации - Строка
//  * ИдентификаторДокумента - Строка
Функция СвойстваПоФайлу(ФайлСообщения) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.Текст =
		"ВЫБРАТЬ РАЗРЕШЕННЫЕ
		|	СообщениеЭДО.ЭлектронныйДокумент КАК ЭлектронныйДокумент,
		|	СообщениеЭДО.ТипЭлементаРегламента КАК ТипЭлементаРегламента,
		|	ЕСТЬNULL(ЭлектронныйДокументВходящийЭДО.ИдентификаторОрганизации,
		|		ЕСТЬNULL(ЭлектронныйДокументИсходящийЭДО.ИдентификаторОрганизации, """")) КАК ИдентификаторОрганизации,
		|	ЕСТЬNULL(ЭлектронныйДокументВходящийЭДО.ИдентификаторДокумента,
		|		ЕСТЬNULL(ЭлектронныйДокументИсходящийЭДО.ИдентификаторДокумента, """")) КАК ИдентификаторДокумента
		|ИЗ
		|	Справочник.СообщениеЭДОПрисоединенныеФайлы КАК СообщениеЭДОПрисоединенныеФайлы
		|		ВНУТРЕННЕЕ СОЕДИНЕНИЕ Документ.СообщениеЭДО КАК СообщениеЭДО
		|		ПО СообщениеЭДОПрисоединенныеФайлы.ВладелецФайла = СообщениеЭДО.Ссылка
		|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ЭлектронныйДокументВходящийЭДО КАК ЭлектронныйДокументВходящийЭДО
		|		ПО СообщениеЭДО.ЭлектронныйДокумент = ЭлектронныйДокументВходящийЭДО.Ссылка
		|		ЛЕВОЕ СОЕДИНЕНИЕ Документ.ЭлектронныйДокументИсходящийЭДО КАК ЭлектронныйДокументИсходящийЭДО
		|		ПО СообщениеЭДО.ЭлектронныйДокумент = ЭлектронныйДокументИсходящийЭДО.Ссылка
		|ГДЕ
		|	СообщениеЭДОПрисоединенныеФайлы.Ссылка = &ФайлСообщения";
	
	Запрос.УстановитьПараметр("ФайлСообщения", ФайлСообщения);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	Выборка = РезультатЗапроса.Выбрать();
	
	Свойства = Новый Структура;
	Свойства.Вставить("ЭлектронныйДокумент", Документы.ЭлектронныйДокументВходящийЭДО.ПустаяСсылка());
	Свойства.Вставить("ТипЭлементаРегламента", Перечисления.ТипыЭлементовРегламентаЭДО.ПустаяСсылка());
	Свойства.Вставить("ИдентификаторОрганизации", "");
	Свойства.Вставить("ИдентификаторДокумента", "");
	
	Если Выборка.Следующий() Тогда
		ЗаполнитьЗначенияСвойств(Свойства, Выборка);
	КонецЕсли;
	
	Возврат Свойства;
	
КонецФункции

// Свойства универсального сообщения ЭДО.
// 
// Параметры:
//  Сообщение - ДокументСсылка.СообщениеЭДО
// 
// Возвращаемое значение:
//  - Неопределено
//  - См. НовыеСвойстваУниверсальногоСообщения
Функция СвойстваУниверсальногоСообщенияЭДО(Сообщение) Экспорт
	
	СписокСообщений = ОбщегоНазначенияКлиентСервер.ЗначениеВМассиве(Сообщение);
	СвойстваУниверсальныхСообщенийЭДО = СвойстваУниверсальныхСообщенийЭДО(СписокСообщений);
	Возврат СвойстваУниверсальныхСообщенийЭДО[Сообщение];
	
КонецФункции

// Параметры:
//  Сообщения - Массив из ДокументСсылка.СообщениеЭДО
// 
// Возвращаемое значение:
//  Соответствие из КлючИЗначение:
//  * Ключ - ДокументСсылка.СообщениеЭДО
//  * Значение См. НовыеСвойстваУниверсальногоСообщения
Функция СвойстваУниверсальныхСообщенийЭДО(Сообщения) Экспорт
	
	Результат = Новый Соответствие;
	
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	СообщениеЭДО.Ссылка КАК Сообщение,
	|	СообщениеЭДО.ОсновнойФайл КАК ОсновнойФайл,
	|	ЕСТЬNULL(СообщениеЭДО.РодительскоеСообщение.ОсновнойФайл.ПолноеИмяФайла, """") Как ИмяФайлаНазначения
	|ИЗ
	|	Документ.СообщениеЭДО КАК СообщениеЭДО
	|ГДЕ
	|	СообщениеЭДО.Ссылка В (&Сообщения)
	|	И СообщениеЭДО.ТипЭлементаРегламента В (&ТипыУниверсальныхСообщений)";
	
	ТипыУниверсальныхСообщений = Новый Массив; // Массив из ПеречислениеСсылка.ТипыЭлементовРегламентаЭДО
	ТипыУниверсальныхСообщений.Добавить(Перечисления.ТипыЭлементовРегламентаЭДО.УС_ДополнительнаяИнформация);
	ТипыУниверсальныхСообщений.Добавить(Перечисления.ТипыЭлементовРегламентаЭДО.УС_ИОП);
	ТипыУниверсальныхСообщений.Добавить(Перечисления.ТипыЭлементовРегламентаЭДО.УС_ОтказВПодписи);
	ТипыУниверсальныхСообщений.Добавить(Перечисления.ТипыЭлементовРегламентаЭДО.УС_УОУ);
	
	Запрос.УстановитьПараметр("ТипыУниверсальныхСообщений", ТипыУниверсальныхСообщений);
	Запрос.УстановитьПараметр("Сообщения", Сообщения);
	
	РезультатЗапроса = Запрос.Выполнить();
	
	ВыборкаДетальныеЗаписи = РезультатЗапроса.Выбрать();
	
	СообщенияОсновныхФайлов = Новый Соответствие;
	ОсновныеФайлыСообщений = Новый Массив;
	Пока ВыборкаДетальныеЗаписи.Следующий() Цикл
		Если ЗначениеЗаполнено(ВыборкаДетальныеЗаписи.ИмяФайлаНазначения) Тогда

			СвойстваСообщения = НовыеСвойстваУниверсальногоСообщения();
			ЗаполнитьЗначенияСвойств(СвойстваСообщения, ВыборкаДетальныеЗаписи);
			Результат.Вставить(ВыборкаДетальныеЗаписи.Сообщение, СвойстваСообщения);
		Иначе
			ОсновныеФайлыСообщений.Добавить(ВыборкаДетальныеЗаписи.ОсновнойФайл);
			СообщенияОсновныхФайлов.Вставить(ВыборкаДетальныеЗаписи.ОсновнойФайл, ВыборкаДетальныеЗаписи.Сообщение);
		КонецЕсли;
	КонецЦикла;
	
	Если ЗначениеЗаполнено(ОсновныеФайлыСообщений) Тогда
		ДвоичныеДанныеФайлов = РаботаСФайлами.ДвоичныеДанныеФайлов(ОсновныеФайлыСообщений);
		Для Каждого КлючЗначение Из ДвоичныеДанныеФайлов Цикл
			ОсновнойФайл = КлючЗначение.Ключ;
			ДанныеОсновногоФайла = КлючЗначение.Значение;
			Сообщение = СообщенияОсновныхФайлов[ОсновнойФайл];

			СвойстваУниверсальногоСообщенияПоФайлу = СвойстваУниверсальногоСообщенияПоФайлу(ДанныеОсновногоФайла);
			Если ЗначениеЗаполнено(СвойстваУниверсальногоСообщенияПоФайлу) Тогда
				Результат.Вставить(Сообщение, СвойстваУниверсальногоСообщенияПоФайлу);
			КонецЕсли;
		КонецЦикла;
	КонецЕсли;

	Возврат Результат;
		
КонецФункции

// Параметры:
//  ДанныеОсновногоФайла - ДвоичныеДанные
// 
// Возвращаемое значение:
//  Неопределено,
//  НовыеСвойстваУниверсальногоСообщения
Функция СвойстваУниверсальногоСообщенияПоФайлу(ДанныеОсновногоФайла) Экспорт
	Результат = Неопределено;
	Если ДанныеОсновногоФайла = Неопределено Тогда
		Возврат Результат;
	КонецЕсли;
	
	ПараметрыСообщенияУС = КонвертацияЭДО.ПолучитьПараметрыЭлектронногоДокумента(ДанныеОсновногоФайла);
	ПараметрыУСОпределеныПоПравилам = ЗначениеЗаполнено(ПараметрыСообщенияУС) 
		И ПараметрыСообщенияУС.Свойство("СвойстваДокументовПоФорматам") 
		И ПараметрыСообщенияУС.СвойстваДокументовПоФорматам.Свойство("УниверсальноеСообщение") 
		И ЗначениеЗаполнено(ПараметрыСообщенияУС.СвойстваДокументовПоФорматам.УниверсальноеСообщение.ИмяФайлаНазначения);
	
	Если ПараметрыУСОпределеныПоПравилам Тогда
		СвойстваСообщения = НовыеСвойстваУниверсальногоСообщения();
		ЗаполнитьЗначенияСвойств(СвойстваСообщения,
			ПараметрыСообщенияУС.СвойстваДокументовПоФорматам.УниверсальноеСообщение);
		Результат = СвойстваСообщения;
	Иначе

		ДанныеСообщения = ОбменСКонтрагентами.ДанныеЭлектронногоДокументаПоФайлу(ДанныеОсновногоФайла);
		Если ЗначениеЗаполнено(ДанныеСообщения) 
			И ДанныеСообщения.НовыйЭД <> Неопределено 
			И ТипЗнч(ДанныеСообщения.НовыйЭД.ЗначениеРеквизита) = Тип("ОбработкаОбъект.ФорматУниверсальноеСообщениеЭДО") Тогда
				
			ОбработкаУС = ДанныеСообщения.НовыйЭД.ЗначениеРеквизита;

			СвойстваСообщения = НовыеСвойстваУниверсальногоСообщения();
			ЗаполнитьЗначенияСвойств(СвойстваСообщения, ОбработкаУС.СвойстваУниверсальногоСообщения());
			Результат = СвойстваСообщения;
		КонецЕсли;
	КонецЕсли;
	
	Возврат Результат;
КонецФункции

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Параметры:
//  ЭлектронныйДокумент - ДокументСсылка.ЭлектронныйДокументВходящийЭДО,ДокументСсылка.ЭлектронныйДокументИсходящийЭДО
// 
// Возвращаемое значение:
//  СправочникСсылка.СообщениеЭДОПрисоединенныеФайлы
// 
Функция ОсновнойФайлИнформацииОтправителя(ЭлектронныйДокумент) Экспорт
	
	ПервыйТитул = Перечисления.ТипыЭлементовРегламентаЭДО.ИнформацияОтправителя;
	Если ОбщегоНазначения.ПодсистемаСуществует("ЭлектронноеВзаимодействие.ОбменСГИСЭПД") Тогда
		МодульОбменСГИСЭПД = ОбщегоНазначения.ОбщийМодуль("ОбменСГИСЭПД");
		РеквизитыДокумента = ОбщегоНазначения.ЗначенияРеквизитовОбъекта(ЭлектронныйДокумент, "ТипРегламента, ВидДокумента");
		Если МодульОбменСГИСЭПД.ЭтоТипРегламентаЭПД(РеквизитыДокумента.ТипРегламента) Тогда
			ТипДокумента = ОбщегоНазначения.ЗначениеРеквизитаОбъекта(РеквизитыДокумента.ВидДокумента, "ТипДокумента");
			ПервыйТитул = МодульОбменСГИСЭПД.ПервыйТитулДокумента(ТипДокумента);
		КонецЕсли;
	КонецЕсли;
		
	Возврат ОсновнойФайлПоТипуЭлементаРегламента(ЭлектронныйДокумент, ПервыйТитул);
	
КонецФункции

// Параметры:
//  ЭлектронныйДокумент - ДокументСсылка.ЭлектронныйДокументВходящийЭДО,ДокументСсылка.ЭлектронныйДокументИсходящийЭДО
//  ТипЭлементаРегламента - ПеречислениеСсылка.ТипыЭлементовРегламентаЭДО
// 
// Возвращаемое значение:
//  СправочникСсылка.СообщениеЭДОПрисоединенныеФайлы
// 
Функция ОсновнойФайлПоТипуЭлементаРегламента(ЭлектронныйДокумент, ТипЭлементаРегламента) Экспорт
	
	Запрос = Новый Запрос;
	Запрос.УстановитьПараметр("ЭлектронныйДокумент", ЭлектронныйДокумент);
	Запрос.УстановитьПараметр("ТипЭлементаРегламента", ТипЭлементаРегламента);
	
	Запрос.Текст =
	"ВЫБРАТЬ
	|	СообщениеЭДО.ОсновнойФайл КАК ОсновнойФайл
	|ИЗ
	|	Документ.СообщениеЭДО КАК СообщениеЭДО
	|ГДЕ
	|	СообщениеЭДО.ЭлектронныйДокумент = &ЭлектронныйДокумент
	|	И СообщениеЭДО.ТипЭлементаРегламента = &ТипЭлементаРегламента";
	
	Выборка = Запрос.Выполнить().Выбрать();
	
	Если Выборка.Следующий() Тогда
		Возврат Выборка.ОсновнойФайл;
	КонецЕсли;
	
	Возврат Неопределено;
	
КонецФункции

// Параметры:
//  Сообщение - ДокументСсылка.СообщениеЭДО
// Возвращаемое значение:
//  ДокументСсылка.ЭлектронныйДокументИсходящийЭДО,ДокументСсылка.ЭлектронныйДокументВходящийЭДО
Функция ЭлектронныйДокумент(Сообщение) Экспорт
	Возврат ОбщегоНазначения.ЗначениеРеквизитаОбъекта(Сообщение, "ЭлектронныйДокумент");
КонецФункции

// Параметры:
//  СвойстваСообщения - Структура:
//  * Дата - Дата
//  * ВидСообщения - СправочникСсылка.ВидыДокументовЭДО
//  ЭтоНовый - Булево - Признак нового сообщения.
// 
// Возвращаемое значение:
//  Строка
Функция ПредставлениеПоСвойствам(СвойстваСообщения, ЭтоНовый = Ложь) Экспорт
	Возврат СтрШаблон(НСтр("ru = '%1 от %2'"), СвойстваСообщения.ВидСообщения, Формат(СвойстваСообщения.Дата, "ДЛФ=D;"));
КонецФункции

// Возвращаемое значение:
//  Структура:
// * ИмяФайлаНазначения - Строка
// * ТекстСообщения - Строка
// * Статус - Строка
Функция НовыеСвойстваУниверсальногоСообщения()
	Результат = Новый Структура;
	Результат.Вставить("ИмяФайлаНазначения", "");
	Результат.Вставить("ТекстСообщения", "");
	Результат.Вставить("Статус", "");
	Возврат Результат;
КонецФункции

#КонецОбласти

#КонецЕсли