#Если Сервер Или ТолстыйКлиентОбычноеПриложение Или ВнешнееСоединение Тогда

#Область ОбработчикиСобытий

Процедура ПередЗаписью(Отказ, Замещение)
	
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

Процедура ПриЗаписи(Отказ, Замещение)
	
	РегистрыСведений.КоличествоЗаписейРегистраторовРасчетов.ОтразитьКоличествоЗаписейРегистраторовРасчетов(
		Отбор.Регистратор.Значение, "РасчетыСКлиентамиПланОтгрузок", ОбменДанными.Загрузка);
		
	Если ОбменДанными.Загрузка Тогда
		Возврат;
	КонецЕсли;
	
КонецПроцедуры

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

Процедура ЗагрузитьСОбработкой(ТаблицаРасчетов) Экспорт
	
	ВзаиморасчетыПроведение.ДобавитьЗаполнитьПорядокОперации(ТаблицаРасчетов, ТипЗнч(Отбор.Регистратор.Значение),);
	Загрузить(ТаблицаРасчетов);
	
КонецПроцедуры

#КонецОбласти

#КонецЕсли